#include "main.h"

/**
 *_isdigit - checks for a digit
 *@c: type integer
 *Return: 1 if c is a digit, 0 if otherwise
 */

int _isdigit(int c)
{
	if (i >= '0' && i <= '9')
	{
	return (1);
	}
	return (0);
}
